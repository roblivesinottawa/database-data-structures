-- Add a new table and create a foreign key constraintSELECT * FROM Employees;CREATE TABLE QuarterlyBonus (    BonusID INT IDENTITY (1,1) NOT NULL PRIMARY KEY,    BonusAmount DECIMAL (10,2),    Quarter CHAR(7),    EmployeeID INT);ALTER TABLE QuarterlyBonusADD CONSTRAINT fk_employees_employeeid FOREIGN KEY (EmployeeID)REFERENCES Employees (EmployeeID);-- Test the foreign key constraintINSERT INTO QuarterlyBonus (BonusAmount, Quarter, EmployeeID)VALUES ('500.00', '2020-Q1', 1000);SELECT * FROM Employees;SELECT * FROM QuarterlyBonus;-- Try to add a bonus for a non-existing employee IDINSERT INTO QuarterlyBonus (BonusAmount, Quarter, EmployeeID)VALUES ('500.00', '2020-Q1', 2000);